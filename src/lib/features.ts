/**
 * Interruptores de funciones. Permiten esconder de la interfaz una función que
 * todavía no está lista para enseñarse (p. ej. en una demo pública) sin borrar
 * su código: basta poner el flag en `true` para volver a mostrarla.
 */

/**
 * Misiones (main quest mensual + misiones semanales). Sin terminar: se oculta
 * de la navegación y del banner de "Hoy". El código de la vista se conserva.
 */
export const MISSIONS_ENABLED = false
